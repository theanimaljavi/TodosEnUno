<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.javigu.todosenuno">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TodosEnUno">
        <activity android:name=".TRESenRaya.tresEnRaya_Nombres"></activity>
        <activity android:name=".TRESenRaya.tresEnRaya_jugar" />
        <activity android:name=".Sudoku.sudoku_jugar" />
        <activity android:name=".Brisca.brisca_jugar" />
        <activity android:name=".Ahorcado.DosJugadores" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Ahorcado.ElegirTematica" />
        <activity android:name=".ElegirJuego" />
    </application>

</manifest>